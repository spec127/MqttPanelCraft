<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">
    
    <TextView 
        android:text="Properties" 
        android:textSize="18sp" 
        android:textStyle="bold" 
        android:textColor="@color/sidebar_section_header"
        android:layout_marginBottom="16dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
        
    <!-- Name/Label REMOVED per user request -->
    <!-- But we need a hidden field or similar if logic depends on it, but user said "Keep Size, Color, Topic". -->
    <!-- Wait, "Name" is often the label on the component. removing it might make it impossible to rename. -->
    <!-- User said: "keep size, color, topic, test topic". -->
    <!-- I will hide Name effectively or put it at bottom if needed, but user instruction is explicit. -->
    <!-- Actually, simpler to just start with Topic which is most important. -->
    
    <!-- Topic (Control - Writable now?) -->
    <!-- Wait, user said "topic" and "test topic". -->
    <!-- In previous code, 'topicConfig' was test topic? And 'tvPropTopic' was read-only? -->
    <!-- Let's make Topic editable here. -->

    <!-- Topic Config (Main Topic) -->
    <com.google.android.material.textfield.TextInputLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="MQTT Topic"
        android:layout_marginBottom="8dp">
        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etPropTopicConfig"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
    </com.google.android.material.textfield.TextInputLayout>
    
    <!-- Test Topic / Payload? User said "test topic". Assuming they mean payload or a secondary topic? -->
    <!-- The previous code had etPropTopicConfig labeled "Test Topic". -->
    <!-- I will assume the main topic is key. Let's look at fields available. -->
    <!-- We have etPropWidth, etPropHeight, etPropColor, etPropTopicConfig. -->
    <!-- No separate 'Test Topic' was in the code before, `etPropTopicConfig` WAS the topic. -->
    <!-- I will interpret user's "topic" as `etPropTopicConfig` and maybe they want a "Payload" field? -->
    <!-- Or maybe "Topic" and "Test Message"? -->
    
    <!-- Let's stick to what we have but clean up. -->

    <!-- W/H Row -->
     <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:baselineAligned="false"
        android:layout_marginBottom="8dp">
         <com.google.android.material.textfield.TextInputLayout
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:hint="Width (dp)"
            android:layout_marginEnd="8dp">
            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/etPropWidth"
                android:inputType="number"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"/>
        </com.google.android.material.textfield.TextInputLayout>
        
        <com.google.android.material.textfield.TextInputLayout
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:hint="Height (dp)">
            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/etPropHeight"
                android:inputType="number"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"/>
        </com.google.android.material.textfield.TextInputLayout>
    </LinearLayout>
    
    <!-- Color -->
    <com.google.android.material.textfield.TextInputLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Color (Hex)"
        android:layout_marginBottom="8dp">
        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etPropColor"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Hidden Name Field just in case needed by logic -->
    <EditText
        android:id="@+id/etPropName"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"/>

    <Button
        android:id="@+id/btnSaveProps"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Apply"/>
        
</LinearLayout>
